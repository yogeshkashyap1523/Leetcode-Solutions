class Solution {
public:
    double myPow(double x, int n) {
        double sol=1;
        if(sol > 0)
        {
            sol = pow(x,n);
        }
        else
        {
            sol =pow(x,n);
        } 
        return sol;
    }
};